services:
  web:
    build:
      context: ../web
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ../web:/app
      - /app/node_modules
    depends_on:
      - api
