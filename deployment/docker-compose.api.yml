services:
  api:
    build:
      context: ../api
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${API_PORT}:80"
    depends_on:
      db:
        condition: service_healthy