services:
  api:
    build:
      context: ../api
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8000:80"
    depends_on:
      db:
        condition: service_healthy
      keycloak-config-cli:
        condition: service_completed_successfully
